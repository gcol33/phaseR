% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_phaseR}
\alias{fit_phaseR}
\title{Fit a phase model}
\usage{
fit_phaseR(
  model,
  data,
  chains = 4,
  iter = 2000,
  warmup = floor(iter/2),
  backend = c("hmc", "laplace"),
  cores = 1,
  seed = NULL,
  ...
)
}
\arguments{
\item{model}{A \code{phase_model} object}

\item{data}{A data frame with columns: id, time, outcome(s), covariates}

\item{chains}{Number of MCMC chains (default 4)}

\item{iter}{Total iterations per chain (default 2000)}

\item{warmup}{Warmup iterations per chain (default iter/2)}

\item{backend}{Estimation backend: "hmc" (default) or "laplace"}

\item{cores}{Number of cores for parallel chains (default 1)}

\item{seed}{Random seed for reproducibility}

\item{...}{Additional arguments passed to backend}
}
\value{
A \code{phase_fit} object
}
\description{
Fit a phase model
}
\examples{
\dontrun{
model <- phase_model(
  phases("inactive", "active"),
  transition("inactive", "active", ~ x),
  dynamics("inactive", y ~ 1),
  dynamics("active", y ~ x)
)
dat <- sim_phaseR(n_units = 50, n_times = 5)
fit <- fit_phaseR(model, data = dat)
}

}
